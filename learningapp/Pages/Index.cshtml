@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="container-sm">

      <h1>This is a list of Courses - version 1</h1>    

    <table class="table table-striped table-bordered">
        <thead class="table-dark">
          <tr>
            <th class="th-sm" scope="col">Course ID 1 </th>
            <th class="th-sm" scope="col">Course Name 1 </th>
            <th class="th-sm" scope="col">Rating 1 </th>            
          </tr>
        </thead>
         
          <tbody>
            @foreach(Course course in @Model.Courses)
            {
            <tr>
            <th scope="row">@course.CourseID</th>
            <td>@course.CourseName</td>
            <td>@course.Rating</td>            
          </tr>
          }

          </tbody>  
</div>